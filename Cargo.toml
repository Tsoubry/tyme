[package]
name = "tyme"
version = "1.2.1"
edition = "2024"
readme = "README.md"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
yew = { version = "0.22", features = ["csr"]}
js-sys = "0.3.104"
gloo = "0.12" # todo: use smaller like gloo_utils, gloo_console,...
wasm-bindgen = "0.2.127"

[dependencies.web-sys]
version = "0.3.104"
features = [
        "Document",
        "HtmlInputElement", 
        "Event", 
        "MouseEvent",
        "HtmlMediaElement",
        "HtmlLinkElement",
    ] 


[profile.release]
lto = true
opt-level = 's'  # use option z for even smaller wasm file sizes